class Solution {
public:
    int titleToNumber(string c) {
        
        int ans=0;
        
        for (int i=0;i<c.length();i++)
        {
            int temp= c[i]-'A'+1;
            ans= ans*26 + temp;
        }
        return ans;
    }
};
